---
export interface Props {
	title: string;
}

const { title } = Astro.props;
---

<header class="h-20 border w-full flex items-center justify-center">
    <h1 class="text-2xl font-bold">{title}</h1>
</header>
